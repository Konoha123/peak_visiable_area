# AGENTS.md

面向后续开发（含 AI 辅助开发）的项目基准信息。

## 项目概述

计算特定山峰或选中的地理坐标点的可视域（可见范围），并可视化结果。计算必须应用真实高程数据（DEM），并考虑地形遮挡效应。

## 核心需求（用户原始意图）

1. 以某个山峰或选中的地理坐标点作为观测点。
2. 计算该点在多大范围内可见（可视域 / Viewshed 计算）。
3. 高程数据必须应用到计算过程中。
4. 计算需考虑地形遮挡效应（视线被地形遮挡则不可见）。
5. 计算结果需要可视化（最终呈现为地图界面）。
6. 【追加需求】在地图界面上点选任意地理坐标点，计算从该点原地向上飞行多少高度，才能看见最初设定的观测点（两点间通视，视线不被地形遮挡）。本质是求解点选位置相对于目标观测点所需抬升的最小高度，同样基于 DEM 并考虑地形遮挡。
7. 【追加需求 2】提供“附近制高点搜索”辅助工具：以选中点为中心、手动选定半径，自动搜索范围内最高 DEM 格网点作为候选，经用户显式采用成为观测点；搜索所用高程数据与当前所设 DEM 数据源同源。

## 交互流程约定（已与用户逐项确认）

1. **坐标输入**：观测点坐标支持两种来源，以**输入方式切换控件**显式选择（默认文本框输入），校验与生效逻辑取决于当前输入模式，均经【确认输入】按钮进入后续流程：
   - **文本框输入**：输入经纬度；支持十进制度与度分秒（DMS）两种格式，需显式切换并校验（格式切换仅在本模式有效），非法输入不得进入后续流程。点击【确认输入】按钮后进行校验：若非法，在按钮旁以一行文字直接显示报错；若通过，同样以一行文字显示“校验成功”或类似字样，并进入后续流程。
   - **地图点选输入**：切至本模式后地图点击即采集观测点坐标（期间“点选测高”暂时不可用；**临时解除**平移/缩放约束，按 Esc 或切回文本框模式退出且不改变状态，退出后按当前观测点恢复约束）。坐标显示于**独立只读文本框**（十进制度，重新点选覆盖）。点击【确认输入】后：有坐标则免格式校验直接“校验成功”，随后**自动切回文本框输入模式**（恢复测高语义与约束，并清空点选状态）；为空则报错提示需先点选。
   - **阶段状态指示与显式切换**：页面醒目显示三态阶段——**选点中**（点击＝采集观测点）／**可测高**（观测点已确认，点击＝查最小抬升）／**未设定观测点**（点击提示先设定）；指示处配切换按钮（非选点阶段“进入地图选点”／选点阶段“退出选点”，等效 Esc），与“输入方式”切换控件联动控制同一模式状态。
2. **曲率上限范围**：确认后在地图窗口显示仅受地球曲率约束的最大可能可见范围（不考虑地形遮挡）：
   - 观测有效高度取观测点处 DEM 高程，据此计算地平线圆半径；不考虑远端目标自身高度的延伸效应。
   - 地球半径模型（纯几何 R / 标准大气折射有效半径 ≈ 7/6 R）为设置项，下拉菜单切换，**默认为纯几何 R**。
   - 计算半径过小时兜底为预设最小显示半径 **5 km**。
   - 该范围同时是地图显示范围与缩放的上限。
3. **可视域叠加**：地形遮挡后的实际可视域以带颜色（半透明）填充区域叠加显示。
4. **缩放约束**：显示范围上限为第 2 步范围；最小显示范围采用与最小显示半径一致的兜底机制（当前同为 5 km）。
5. **点选测高**：点击地图内任意点，侧栏显示为看见原观测点所需的最小抬升高度；自点击点 DEM 地面高程向上起算；单位支持米、英尺等，下拉菜单切换；若受地球半径约束几何上不可能通过抬升通视（如两点位于地心两侧；一般化为地心角 ≥ 90°，视线恒穿过地球体），显示固定兜底字符串。正常流程理论上不应触发；DEM 数据不可用等异常提示开发时另行补充。
   - **剖面缩略图**：在抬升高度数值下方渲染两点间测地线地形剖面缩略图：横轴为测地线距离（单位 km），纵轴为沿线 DEM 高程（单位随抬升高度的米/英尺下拉联动），数据即测高算法已采样的同一份剖面，每次点击即时更新；几何不可行（兜底字符串场景）时仍展示剖面（剖面可取数时）并叠加“无解”标注，剖面取数失败（DEM 不可用/超出覆盖）时缩略图区域显示错误占位文本（沿用“DEM 异常提示开发时补充”口径）；更换观测点后随旧抬升结果一并清除（归入第 9 条“关联显示清除”）。
   - **测地线连线渲染**：完成一次点选测高后，把观测点与点选点之间的测地线（与剖面采样所用同一条）渲染到地图上；再次点击测高即被新连线覆盖（仅保留最新一条）；几何不可行（兜底字符串场景，测高有结果）同样渲染，DEM 取数失败等测高未完成的场景不渲染；更换观测点后随旧抬升结果一并清除（归入第 9 条“关联显示清除”）；线型样式（颜色/粗细等）开发时定。

6. **底图来源**：作为设置项开放，支持在线瓦片与离线文件两种方式切换，**默认为在线瓦片**。
7. **高级设置（DEM 数据源）**：DEM 数据源以【高级设置】下拉菜单开放，选项含内置常见在线服务（候选：Copernicus DEM GLO-30/90、SRTM GL1/GL3、AWS Terrain Tiles 等，最终清单开发时定）与用户自选离线文件。
8. **高级设置（Viewshed 引擎）**：计算引擎下拉切换——自研角向射线扫描 / GDAL Viewshed，界面上配有文字对各选项作简单说明（默认引擎开发时定）。
9. **观测点更换**：确认新观测点后自动替换旧结果（旧可视域及关联显示清除，以新计算结果替代）。
10. **附近制高点搜索（追加需求 2）**：在选定半径的圆形范围内搜索最高高程格网点作为候选。搜索范围经下拉框手动选择，档位 0.5 / 1 / 2 / 5 / 10 / 25 km，**默认 2 km**，两工具各配**独立**下拉（取值互不同步）。UI 层面为**两个用途独立的辅助工具**（不约束内部实现是否共用）：一者服务于**选被观测点**，一者服务于**测高（选择测高点；“测高点”＝第 5 条点选测高的点击点）**。
    - **选点辅助**（工具一，确认前）：存在待确认选中坐标时可用——地图点选模式＝只读框中已点选坐标；文本框模式＝文本框当前内容通过即时格式校验（与【确认输入】同一校验逻辑，非法则入口禁用）。搜索中心＝该坐标，无有效中心则禁用。地图点选模式下触发搜索不改变地图点击的点选语义。
    - **测高点辅助**（工具二，可测高阶段）：搜索中心＝**最近一次测高点击点**；未设定观测点、处于选点模式（点选测高暂不可用期间）或尚无任何测高点击时禁用。两工具互不依赖、互不替代。
    - **制高点定义**：范围内 DEM 最大高程所在格网单元中心（十进制度返回）；**不做**山头显著性（prominence）过滤；同高平局取距中心最近格点；中心自身即最高时返回中心并提示“当前点已是附近制高点”。
    - **数据同源**：搜索用高程数据与当前【高级设置】DEM 数据源同源（在线 AWS Terrain Tiles / 离线 GeoTIFF），一律取触发搜索时点的当前设置；高程语义与其余取数一致接入陆海掩膜钳制（成为第四处统一接入点）。
    - **结果呈现**：侧栏显示候选坐标、高程（随米/英尺下拉联动）、与中心距离（固定 km）；地图绘制候选点 marker 与虚线搜索范围圆（两工具样式相互区分，并与既有观测点/点击点标记区分）；**不自动平移地图**。搜索半径可大于当前缩放约束上限，此时候选 marker 可能暂不可见属预期（侧栏仍完整显示结果；工具一采用后地图以新观测点重定中心即可见），不为此放宽第 2/4 条缩放约束。再次搜索覆盖旧结果（仅保留最新一组）。
    - **采用交互（按工具分流；均不自动采用、不静默替换；中心即最高时隐藏采用按钮）**：
      - 工具一结果旁提供【采用为观测点】按钮：以候选坐标走既有【确认输入】生效链路（等效于以该坐标完成一次确认：观测点更换、旧结果清除、可视域重算、点选模式自动退出等均由既有条款接管）。
      - 工具二结果旁提供【采用为测高点】按钮：**程序化等效于点击该候选点**——复用第 5 条点选测高管线（最小抬升高度、剖面缩略图、测地线连线、点击点 marker 均由既有链路接管并按其覆盖规则更新），“最近一次测高点击点”随之更新为该候选；**不更换观测点、不改坐标输入框**。
    - **覆盖不足**：搜索圆部分超出 DEM 覆盖（离线文件范围不足/无效值）时 best-effort 在有效覆盖内搜索，覆盖率偏低时附提示；零有效数据按 DEM 取数失败处理（沿用现有 DEMError 提示链路）。
    - 候选 marker 与搜索范围圆纳入第 9 条“关联显示清除”清单（更换观测点、重新搜索、采用后清除）。
    - **勘误（2026-09）**：本条工具二最初定为“观测点微调辅助”（中心＝已确认观测点、采用＝更换观测点），源于对用户“两个环节独立的辅助工具”表述的误读（误作“确认前/确认后”两个阶段，实为“选被观测点/测高”两个用途）；已按用户澄清修正为“测高点辅助”，工具一用途与用法不变。同批澄清：两工具半径下拉为各自独立、互不同步。

## 技术方案决策

### 已确认

1. **DEM 数据源形态**：见交互流程第 7 条（高级设置下拉：在线服务 + 用户离线文件）。
2. **点选测高算法**：沿两点间测地线按所用 DEM 的分辨率采样地形剖面，一维求根（二分/切线法）求解最小抬升高度；大气折射与设置项联动纳入计算。
3. **视线模型统一**：所有视线/遮挡计算统一采用有效地球半径模型，与折射设置联动（纯几何 R / 折射有效半径 ≈ 7/6 R）。
4. **高程基准**：主流 DEM 源均为正高体系（Copernicus DEM：EGM2008；SRTM、AWS Terrain Tiles：EGM96），基准以服务文档/产品规范标注为准（一般不在瓦片数据本身）；EGM96 与 EGM2008 差异典型 <1.5 m，对通视判定影响可忽略，不做椭球高转换，按源记录基准即可。
5. **计算时序**：可视域为同步计算（确认输入后即时算出并叠加显示）。
6. **Viewshed 引擎**：双实现并存——自研角向射线扫描（主，配合分级分辨率采样满足同步秒级延迟）与 GDAL Viewshed（备选/校验），经【高级设置】下拉切换，界面附简单文字说明。
7. **旧结果清除**：更换观测点时自动替换（见交互流程第 9 条）。
8. **跨系统可移植性（约束）**：自用场景下仍要求更换操作系统后项目可运行：依赖须为跨平台包（Windows/macOS/Linux 均可安装），避免绑定特定 OS 的组件；前端资源经 CDN 加载不依赖系统环境；离线数据采用标准格式（GeoTIFF 等本地文件）。
9. **GUI 技术栈**：Web 方案——后端 Python + FastAPI，前端 Leaflet；前端免构建（Leaflet 资源优先经 CDN 加载，另内置静态文件作离线兜底），目标机器仅需 Python 环境与现代浏览器，无任何前端开发环境要求。
10. **点选测高剖面缩略图显示方案**：采用地球曲率修正基准线（弦线基准）而非直线 x-y 剖面：地形显示值为 z(d) **+** d·(D−d)/(2·R_eff)（地球凸起向上，平地呈两端低中间高的弧线；R_eff 与折射设置联动，默认纯几何 R），并叠加“抬升后点击点→观测点”的视线段——真实空间中视线为直线，在弦线基准下恰好仍画为两端点直连线段（这是唯一能同时满足“视线为直线”且“视线端点锚定剖面两端点”的显示基准），两线同基准后最小抬升时视线恰与剖面相切，直观解释平地仍需抬升的物理原因。图内附图注，说明曲率修正量（中点凸起 ≈ D²/8R，随米/英尺联动）与视线含义。依据（临时脚本估算，非推测）：典型点选距离（观测点高程对应地平线半径约 80–320 km）下剖面中点凸起达 50–1800 m、占图高 10%–90%+，与直线显示差异肉眼显著；抛物近似相对精确圆弧 sagitta 误差 <0.02%（D=300 km），可直接用于渲染。
    - **勘误（2026-09）**：本条公式符号曾误写为减号（z − …，凹陷画法），与“直视线段”组合后地形与视线不在同一显示基准（相差 2·drop），导致最小抬升时蓝线全程悬空、与剖面不相切；凹/凸两种画法几何等价（仅整列平移 2·drop），但仅凸起画法（弦线基准本义）能保持视线为直线，故修正为加号。
11. **附近制高点搜索算法**：复用现有 DEM 取数链路（`fetch_bbox` 取圆形范围外接矩形 + 陆海掩膜钳制），在圆形掩膜内对高程格网求最大值（NumPy `nanargmax` 语义），平局取距中心最近格点，返回格网单元中心坐标。在线源目标格距取近距档（与地平线/测高一致，约 30 m），与可视域大半径分析档（90 m）允许不同——口径为“**数据库同源、分辨率不必相同**”。同步计算（25 km@~30 m 约 1700×1700 格，秒级）。terrarium 编码与重采样伪峰典型 <1 m，不做平滑或显著性过滤；明确排除 topographic prominence 计算（那是“列出多个独立山头”的需求，超出本工具范围）。

## 开发环境

- **conda 环境名**：`peak-visiable-area-dev-env`
  - 该名称以仓库根目录的 `conda_env_name` 文件为准，如不一致以文件内容为准。
  - 激活方式：`conda activate $(cat conda_env_name)`
- 语言：Python；后端框架 FastAPI，前端 Leaflet（具体依赖清单待开发时确定并记录）
- **宿主系统**（2026-09 检测）：Ubuntu 24.04.4 LTS（x86_64，内核 6.8）；X11 桌面会话（DISPLAY 可用），已装 Firefox 与 Google Chrome；12 核 CPU / 15 GB 内存；无可用 NVIDIA GPU；外网连通（PyPI、npm registry、OSM 瓦片、AWS 高程瓦片均可达）；Node v16 存在但 npm 缺失（前端可采用 CDN 免构建方案规避）。项目 conda 环境当前无地理/GUI 相关包。

## 项目状态与约定

- 项目已完成第一版开发（模块：`app/geo/*` 计算核心、`app/api/routes.py` 接口、`static/` 前端），进度见 git 提交历史；交互流程与技术方案基准见上文各节。
- 常用命令（均在仓库根目录执行，先 `conda activate $(cat conda_env_name)`）：
  - 运行：`uvicorn app.main:app --port 8000`（访问 http://127.0.0.1:8000）
  - 测试：`pytest`
  - 前端自检（无头浏览器跑 `/?selftest=1` 并校验全 PASS）：`scripts/frontend_selftest.sh`（需 Chrome/Chromium，端口可用 `PVA_SELFTEST_PORT` 覆盖）
  - Lint：`ruff check app tests`
- 依赖记录：pip 部分见 `requirements.txt`，完整 conda 环境见 `environment.yml`（GDAL 必须经 conda 安装）；新增依赖时须同步更新这两个文件。
- 实现备注：
  - DEM 在线源为 AWS Terrain Tiles（terrarium 编码），拼接后重采样为均匀纬度格网（墨卡托行距非均匀，直接线性配准在大范围会引入数公里偏差）。
  - GDAL 3.13 实测缺陷：绑定 `ViewshedGenerate` 位置参数错位、`gdal_viewshed` 的 `-cc` 曲率系数失效、`-md` 误杀近距离格点；GDAL 引擎采用“子进程 + 曲率预烘焙 + 自管半径”绕开，复杂地形下结果可能偏保守。
  - 点选测高的抬升高度含地球凸起项：平地等高两点 D km 距离需约 D²/(2R) 的基准抬升属正常物理结果。
  - 前端无构建/无 JS 测试框架约束下，提供页面自检模式：访问 `/?selftest=1` 会驱动输入模式切换/阶段指示/自动退出链路并将 PASS/FAIL 写入 DOM（`#selftest-results`），可用无头浏览器 dump-dom 校验。
  - 调试日志：后端用标准 logging（`app.*` 命名空间，INFO=请求/计算摘要，DEBUG=缓存命中等细节，WARNING=DEM 获取/计算失败）；前端统一 `[pva]` 前缀 console.log（关键错误用 console.warn）。
  - 海面语义（陆海掩膜）：DEM 负高程混装海底地形与内陆负海拔洼地，数值无法区分。引入 Natural Earth 50m 陆地多边形（公有领域，首次自动下载缓存至 `~/.cache/pva/assets/`，可用环境变量 `PVA_LAND_GEOJSON` 指定离线文件）经 GDAL 栅格化为陆/海掩膜，仅对**海洋**格点钳制为海平面 0 m（内陆负高程如死海保留原值）；掩膜不可用时不钳制并告警（退化为原始海床行为）。可视域、测高、地平线三处取数统一接入。
  - 剖面缩略图与测地线连线（交互流程第 5 条、技术方案决策第 10 条）已实现：`/api/lift` 响应附带剖面序列（`profile.dist_m/elev_m`，≤2048 点等距抽稀、含首末点）与测地线顶点序列（`geodesic`，≤512 点），与测高求解出自同一组测地线采样；几何不可行分支测地线照常返回、剖面仅在 DEM 全程覆盖时附带。前端按 `z(d) + d(D−d)/(2·R_eff)` 渲染曲率修正剖面（弦线基准、中点凸起）并叠加“抬升后点击点→观测点”视线段（两端点直连线段，与地形同基准，最小抬升时与剖面相切；凹/凸画法勘误见决策第 10 条），图注说明曲率修正量与视线含义，高程轴随米/英尺联动，无解/取数失败有兜底显示；两者均经 `/?selftest=1` 自检覆盖。
  - 附近制高点搜索（交互流程第 10 条、技术方案决策第 11 条）已实现：`POST /api/peak-search`（`app/geo/peak.py` 圆形掩膜内 argmax，平局取距中心最近格点；coverage_ratio＝圆内有效格点数/圆面积折算的格点数期望，天然兼容离线源窗口被裁剪的情形；中心即最高时返回中心原坐标并置 `is_center_highest`；取数接入海面钳制，目标格距 NEAR_CELL_M≈30 m；零有效数据走 DEMError→502）。前端为两个用途独立的辅助工具——选点辅助（嵌入“观测点坐标”卡片，橙色系）与测高点辅助（独立卡片、置于高级设置卡片之后/点选测高卡片之前，青色系），各配**独立**半径下拉（0.5–25 km 六档，默认 2 km）；选点辅助中心＝点选坐标或文本框防抖 400 ms 即时校验（复用 `/api/validate`，不在前端另写解析器）通过的坐标；测高点辅助中心＝最近一次测高点击点（`state.lastLiftClick`，仅测高成功时更新、更换观测点时随 `resetLiftPanel` 清除），未设定观测点、选点模式中或尚无测高点击时禁用；候选 marker＋虚线范围圆绘制于独立 `peakGroup`（两工具颜色区分），不自动平移地图，搜索半径超出当前缩放约束时不放宽约束；任何一次新搜索全局覆盖旧结果（仅保留最新一组），`loadObserver`（更换观测点、采用为观测点、引擎/折射切换触发的重确认）统一清除；采用分流——工具一【采用为观测点】＝候选坐标以十进制 6 位写入文本框（同步切 decimal 格式）＋程序化触发 `onConfirm()` 走既有确认链路，工具二【采用为测高点】＝清候选后调用 `measureLift(候选)`（与地图点击共用的同一管线，最小抬升值/剖面缩略图/测地线连线/点击点 marker 均由其接管，“最近一次测高点击点”随之更新），不更换观测点、不改坐标输入框；覆盖率 <50% 以警告色附提示；均经 `/?selftest=1` 自检覆盖（55 项全 PASS）。
