# AGENTS.md

面向后续开发（含 AI 辅助开发）的项目基准信息。

## 项目概述

计算特定山峰或选中的地理坐标点的可视域（可见范围），并可视化结果。计算必须应用真实高程数据（DEM），并考虑地形遮挡效应。

## 核心需求（用户原始意图）

1. 以某个山峰或选中的地理坐标点作为观测点。
2. 计算该点在多大范围内可见（可视域 / Viewshed 计算）。
3. 高程数据必须应用到计算过程中。
4. 计算需考虑地形遮挡效应（视线被地形遮挡则不可见）。
5. 计算结果需要可视化（最终呈现为地图界面）。
6. 【追加需求】在地图界面上点选任意地理坐标点，计算从该点原地向上飞行多少高度，才能看见最初设定的观测点（两点间通视，视线不被地形遮挡）。本质是求解点选位置相对于目标观测点所需抬升的最小高度，同样基于 DEM 并考虑地形遮挡。

## 开发环境

- **conda 环境名**：`peak-visiable-area-dev-env`
  - 该名称以仓库根目录的 `conda_env_name` 文件为准，如不一致以文件内容为准。
  - 激活方式：`conda activate $(cat conda_env_name)`
- 语言：Python（具体依赖尚未确定，待后续开发时确定）

## 项目状态与约定

- 当前仅完成项目基准文档（README.md、AGENTS.md），**尚未进行任何代码层面的规划与开发**。
- 后续如确定构建/测试/lint 命令，请更新本文件，例如：
  - 构建：TBD
  - 测试：TBD
  - Lint：TBD
- 新增依赖时须同步记录（如 requirements.txt / environment.yml，待定）。
